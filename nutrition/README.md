# Nutrition Integration

Nutrition data is intentionally not populated yet.

The classifier returns a food slug such as `plov`. A later nutrition layer can use
that slug to look up verified nutrient records without changing or retraining the
image model.
